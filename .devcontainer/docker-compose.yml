services:
  db:
    image: mariadb:10.11
    container_name: ${THEME_SLUG:-meros-wp}_dev_db
    environment:
      MARIADB_DATABASE: ${DB_NAME:-wordpress}
      MARIADB_USER: ${DB_USER:-dbuser}
      MARIADB_PASSWORD: ${DB_PASSWORD:-dbpassword}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpassword}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 10

  wordpress:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${THEME_SLUG:-meros-wp}_dev_wordpress
    userns_mode: keep-id
    ports:
      - ${WP_PORT:-8000}:8080
    depends_on:
      db:
        condition: service_healthy

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: ${THEME_SLUG:-meros-wp}_dev_phpmyadmin
    ports:
      - ${PHPMYADMIN_PORT:-9000}:80
    volumes:
      - ./phpmyadmin.inc.php:/etc/phpmyadmin/config.user.inc.php:ro,Z
    environment:
      PMA_HOST: db
      PMA_USER: ${DB_USER:-dbuser}
      PMA_PASSWORD: ${DB_PASSWORD:-dbpassword}

volumes:
  db_data: